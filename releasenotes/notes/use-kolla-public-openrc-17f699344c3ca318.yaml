---
upgrade:
  - |
    Use the ``public-openrc.sh`` generated by Kolla Ansible instead of
    generating our own. This may result in minor changes to this file.
fixes:
  - |
    Fixes wrong values of ``OS_AUTH_URL`` in ``public-openrc.sh`` when using a
    single external frontend in HAProxy.
    `LP#2168467 <https://bugs.launchpad.net/kayobe/+bug/2168467>`__
