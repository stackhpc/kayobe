---
fixes:
  - |
    Stop merging merging config when symlink points to the same file. This
    was a common pattern before the merging of kolla config was introduced
    and results in duplicated config options.
